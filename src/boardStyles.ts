export default `
  svg {
    display: block;
    width: 100%;
    line-height: 0;
  }
  span {
    line-height: 1;
    font-size: 10px;
    color: var(--html-diagram-color);
    line-break: anywhere;
    font-family: var(--html-diagram-font);
  }
`
