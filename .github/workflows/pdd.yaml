name: pdd

"on":
    push:
        branches:
            - main
    pull_request:
        branches:
            - main
jobs:
    pdd:
        timeout-minutes: 15
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: volodya-lombrozo/pdd-action@master
